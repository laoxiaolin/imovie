extends ../../layout

block content
  .container
    .row
      .col-md-6
        embed(src="#{movie.flash}", allowFullScreen="true", quality="high", width="1", height="1", align="middle", type="application/x-shockwave-flash")
        img.img-responsive.img-thumbnail(src=movie.poster alt=movie.title)
        .pull-left 播放视频
        //- .panel.panel-default
        //-   .panel-heading
        //-     h3 评论区
        //-   .panel-body
        //-     ul.media-list
        //-     each item in comments
        //-       li.meida
        //-       .pull-left
        //-         a.comment(href="#comments", data-cid="#{item._id}", data-tid="#{item.from._id}")
        //-           img(src="http://haml.info/images/haml.png", height="30", width="30")
        //-       .media-body
        //-         h4.media-heading #{item.from.name}
        //-         p #{item.content}
        //-         if item.reply && item.reply.length > 0
        //-           each reply in item.reply
        //-             .meida
        //-               .pull-left
        //-                 a.comment(href="#comments", data-cid="#{item._id}", data-tid="#{reply.from._id}")
        //-                   img(src="http://haml.info/images/haml.png", height="30", width="30")
        //-               .media-body
        //-                 h4.media-heading 
        //-                   | #{reply.from.name}
        //-                   span.text-info 回复
        //-                   | #{reply.to.name} :

        //-                 p #{reply.content}
        //-         hr
        //-     hr
        //-   #comments
        //-     form#commentForm(method="POST", action="/user/comment")
        //-       input(type="hidden", name="comment[movie]", value="#{movie._id}")         
        //-       if user
        //-         .form-group
        //-           textarea.from-control(name="comment[content]", rows="5", cols="70")
        //-         input(type="hidden", name="comment[from]", value="#{user._id}")
        //-         button.btn.btn-primary(type="submit") 提交
        //-       else
        //-         a.navbar-link(href="#", data-toggle="modal", data-target="#signinModal") 登陆后评论
      .col-md-6
        dl.dl-horizontal
          dt 电影类型
          dd #{movie.category.name}
          dt 电影名字
          dd #{movie.title}
          dt 导演
          dd #{movie.doctor}
          dt 国家
          dd #{movie.country}
          dt 语言
          dd #{movie.language}
          dt 上映年份
          dd #{movie.year}          
          dt 简介
          dd #{movie.summary}
  script(src="/js/detail.js")